apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${{ values.name }}-pvc
  namespace: ${{ values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: ${{ values.storage }}